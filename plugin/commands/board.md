---
description: Show the actionable OverClick queue and this session's active claims.
---

Load the bundled `overclick` skill and read its linked canonical `OVERCLICK.md`.
Call `task_list` for open cards, then call `task_list`
with `status: "em_execucao"`, `claimed_by: "me"`, and the `session_id` declared
in this session's claim. Never substitute another pane's session. Present short IDs, titles,
priorities, and statuses compactly. Say when either response is truncated.
